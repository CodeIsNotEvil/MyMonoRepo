{
  "//": "Leave ApiBaseUrl empty unless you are running the Blazor dev server on its own port. Empty means 'same origin', which is correct both in production behind nginx and when the BFF hosts the app itself. Set it to http://localhost:5000 only while running `dotnet run` on this project separately; the BFF allows that origin in development.",
  "ApiBaseUrl": ""
}

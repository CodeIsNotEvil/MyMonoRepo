apiVersion: v1
kind: Service
metadata:
  name: ShoppingManager
  namespace: default
  labels:
    app: ShoppingManager
spec:
  ports:
  - port: 80
    protocol: TCP
    name: ShoppingManager-HttpPort
  selector:
    app: ShoppingManager
    tier: frontend
  type: NodePort
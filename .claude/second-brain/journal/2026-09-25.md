---
tags: [journal]
created: 2026-09-25
updated: 2026-09-25
---
# 2026-09-25

- Created the second brain at `.claude/second-brain/` and seeded it from `CLAUDE.md`, the
  GroceryTracker README and the deploy runbooks.
- Noticed that `Scripts/dotnet_project.sh` hardcodes `~/Repositories/MyMonoRepo`, but the checkout is
  at `~/repos/MyMonoRepo`. See [[shoppingmanager]].
- Added a Claude git workflow to `CLAUDE.md`: work on descriptively named branches and open PRs for the
  owner to review, with no direct commits to `main`.
- Added Docker and Podman compose portability rules to `CLAUDE.md`. Found short image names that
  violate them. They're listed in [[podman-compose]].
- PRs #2 and #3 merged. Switched the Keycloak compose to the official `quay.io/keycloak/keycloak:26.7`
  image (the Bitnami one is gone from Docker Hub). Tested with `podman compose`: the realm answered
  and the admin login worked. See [[kubernetes-learning]].
- PR #4 merged. Moved the Keycloak stack's Postgres to the official `postgres:17-alpine` image, with a
  new `keycloak-postgres-data` volume because the Bitnami data layout isn't compatible. I also fixed
  the healthcheck, which checked a `postgres` role that doesn't exist. Verified with Podman.
